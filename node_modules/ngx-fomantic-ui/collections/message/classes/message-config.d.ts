import { EventEmitter } from '@angular/core';
export declare type MessageState = '' | 'info' | 'success' | 'warning' | 'error';
export declare const MessageState: {
    Default: MessageState;
    Info: MessageState;
    Success: MessageState;
    Warning: MessageState;
    Error: MessageState;
};
export declare class MessageConfig {
    text: string;
    header?: string;
    state: MessageState;
    timeout: number;
    extendedTimeout: number;
    hasDismissButton: boolean;
    hasProgress: boolean;
    transition: string;
    transitionInDuration: number;
    transitionOutDuration: number;
    onClick: EventEmitter<void>;
    onDismiss: EventEmitter<void>;
    constructor(text: string, state?: MessageState, header?: string);
}
