import { MessageConfig } from './message-config';
import { FuiActiveMessage } from './active-message';
import { FuiMessageContainer } from '../components/message-container';
export interface IMessageController {
    maxShown: number;
    isNewestOnTop: boolean;
    show(config: MessageConfig): FuiActiveMessage;
    dismissAll(): void;
}
export declare class MessageController implements IMessageController {
    maxShown: number;
    isNewestOnTop: boolean;
    private _container;
    constructor();
    registerContainer(container: FuiMessageContainer): void;
    show(config: MessageConfig): FuiActiveMessage;
    dismissAll(): void;
    private throwContainerError;
}
