import { MessageConfig } from './message-config';
import { ComponentRef } from '@angular/core';
import { FuiMessage } from '../components/message';
export declare abstract class FuiActiveMessage {
    abstract onClick(callback: () => void): FuiActiveMessage;
    abstract onDismiss(callback: () => void): FuiActiveMessage;
    abstract dismiss(): void;
}
export declare class ActiveMessage implements FuiActiveMessage {
    config: MessageConfig;
    componentRef: ComponentRef<FuiMessage>;
    constructor(config: MessageConfig, componentRef: ComponentRef<FuiMessage>);
    readonly component: FuiMessage;
    onClick(callback: () => void): ActiveMessage;
    onDismiss(callback: () => void): ActiveMessage;
    dismiss(): void;
}
